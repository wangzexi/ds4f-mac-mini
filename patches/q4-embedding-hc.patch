--- a/ds4_gpu.h
+++ b/ds4_gpu.h
@@ -366,6 +366,7 @@
         const void       *model_map,
         uint64_t          model_size,
         uint64_t          weight_offset,
+        uint32_t          weight_type,
         uint32_t          n_vocab,
         uint32_t          token,
         uint32_t          n_embd,
@@ -377,6 +378,7 @@
         const void             *model_map,
         uint64_t                model_size,
         uint64_t                weight_offset,
+        uint32_t                weight_type,
         uint32_t                n_vocab,
         uint32_t                n_tokens,
         uint32_t                n_embd,
--- a/ds4_metal.m
+++ b/ds4_metal.m
@@ -9956,6 +9956,7 @@
         const void       *model_map,
         uint64_t          model_size,
         uint64_t          weight_offset,
+        uint32_t          weight_type,
         uint32_t          n_vocab,
         uint32_t          token,
         uint32_t          n_embd,
@@ -9964,7 +9965,72 @@
     if (!out_hc || !model_map || n_vocab == 0 || token >= n_vocab || n_embd == 0 || n_hc == 0) {
         return 0;
     }
+
+    if (weight_type != 1u) {
+        @autoreleasepool {
+            id<MTLBuffer> outbuf = ds4_gpu_tensor_buffer(out_hc);
+            const uint64_t out_bytes = (uint64_t)n_embd * n_hc * sizeof(float);
+            if (!outbuf || ds4_gpu_tensor_bytes(out_hc) < out_bytes) {
+                fprintf(stderr, "ds4: Metal quant embedding received undersized HC output buffer\n");
+                return 0;
+            }
 
+            uint64_t weight_bytes = 0;
+            if (!ds4_gpu_quant_table_bytes(weight_type, n_vocab, n_embd, &weight_bytes) ||
+                weight_offset > model_size || weight_bytes > model_size - weight_offset) {
+                fprintf(stderr, "ds4: Metal quant HC embedding range is outside the mapped model\n");
+                return 0;
+            }
+
+            uint64_t inner_offset = 0;
+            id<MTLBuffer> wbuf = ds4_gpu_wrap_model_range(model_map,
+                                                          model_size,
+                                                          weight_offset,
+                                                          weight_bytes,
+                                                          &inner_offset);
+            if (!wbuf) return 0;
+
+            const NSUInteger row_bytes = (NSUInteger)n_embd * sizeof(float);
+            if (!ds4_gpu_ensure_scratch_buffer(&g_embed_rows_buffer,
+                                                &g_embed_rows_bytes,
+                                                row_bytes,
+                                                "ds4_embed_rows")) {
+                return 0;
+            }
+
+            int owned = 0;
+            id<MTLCommandBuffer> cb = ds4_gpu_command_buffer(&owned);
+            if (!cb) return 0;
+
+            const int32_t token_i32 = (int32_t)token;
+            if (!ds4_gpu_encode_get_rows_quant(cb,
+                                                wbuf,
+                                                (NSUInteger)inner_offset,
+                                                weight_type,
+                                                nil,
+                                                0,
+                                                &token_i32,
+                                                g_embed_rows_buffer,
+                                                0,
+                                                n_vocab,
+                                                1,
+                                                n_embd) ||
+                !ds4_gpu_encode_repeat_hc_embedding(cb,
+                                                     g_embed_rows_buffer,
+                                                     0,
+                                                     outbuf,
+                                                     ds4_gpu_tensor_offset(out_hc),
+                                                     1,
+                                                     n_embd,
+                                                     n_hc)) {
+                return 0;
+            }
+
+            if (!ds4_gpu_finish_command_buffer(cb, owned, "quant HC embed token")) return 0;
+        }
+        return 1;
+    }
+
     @autoreleasepool {
         id<MTLBuffer> outbuf = ds4_gpu_tensor_buffer(out_hc);
         const uint64_t out_bytes = (uint64_t)n_embd * n_hc * sizeof(float);
@@ -10055,6 +10121,7 @@
         const void             *model_map,
         uint64_t                model_size,
         uint64_t                weight_offset,
+        uint32_t                weight_type,
         uint32_t                n_vocab,
         uint32_t                n_tokens,
         uint32_t                n_embd,
@@ -10064,6 +10131,74 @@
         return 0;
     }
 
+    if (weight_type != 1u) {
+        @autoreleasepool {
+            id<MTLBuffer> outbuf = ds4_gpu_tensor_buffer(out_hc);
+            id<MTLBuffer> tokbuf = ds4_gpu_tensor_buffer(tokens);
+            const uint64_t out_bytes = (uint64_t)n_tokens * n_embd * n_hc * sizeof(float);
+            const uint64_t token_bytes = (uint64_t)n_tokens * sizeof(int32_t);
+            if (!outbuf || !tokbuf ||
+                ds4_gpu_tensor_bytes(out_hc) < out_bytes ||
+                ds4_gpu_tensor_bytes(tokens) < token_bytes) {
+                fprintf(stderr, "ds4: Metal quant batched embedding received undersized HC buffers\n");
+                return 0;
+            }
+
+            uint64_t weight_bytes = 0;
+            if (!ds4_gpu_quant_table_bytes(weight_type, n_vocab, n_embd, &weight_bytes) ||
+                weight_offset > model_size || weight_bytes > model_size - weight_offset) {
+                fprintf(stderr, "ds4: Metal quant batched HC embedding range is outside the mapped model\n");
+                return 0;
+            }
+
+            uint64_t inner_offset = 0;
+            id<MTLBuffer> wbuf = ds4_gpu_wrap_model_range(model_map,
+                                                          model_size,
+                                                          weight_offset,
+                                                          weight_bytes,
+                                                          &inner_offset);
+            if (!wbuf) return 0;
+
+            const NSUInteger rows_bytes = (NSUInteger)n_tokens * n_embd * sizeof(float);
+            if (!ds4_gpu_ensure_scratch_buffer(&g_embed_rows_buffer,
+                                                &g_embed_rows_bytes,
+                                                rows_bytes,
+                                                "ds4_embed_rows")) {
+                return 0;
+            }
+
+            int owned = 0;
+            id<MTLCommandBuffer> cb = ds4_gpu_command_buffer(&owned);
+            if (!cb) return 0;
+
+            if (!ds4_gpu_encode_get_rows_quant(cb,
+                                                wbuf,
+                                                (NSUInteger)inner_offset,
+                                                weight_type,
+                                                tokbuf,
+                                                ds4_gpu_tensor_offset(tokens),
+                                                NULL,
+                                                g_embed_rows_buffer,
+                                                0,
+                                                n_vocab,
+                                                n_tokens,
+                                                n_embd) ||
+                !ds4_gpu_encode_repeat_hc_embedding(cb,
+                                                     g_embed_rows_buffer,
+                                                     0,
+                                                     outbuf,
+                                                     ds4_gpu_tensor_offset(out_hc),
+                                                     n_tokens,
+                                                     n_embd,
+                                                     n_hc)) {
+                return 0;
+            }
+
+            if (!ds4_gpu_finish_command_buffer(cb, owned, "quant HC embed tokens")) return 0;
+        }
+        return 1;
+    }
+
     @autoreleasepool {
         id<MTLBuffer> outbuf = ds4_gpu_tensor_buffer(out_hc);
         id<MTLBuffer> tokbuf = ds4_gpu_tensor_buffer(tokens);
--- a/ds4.c
+++ b/ds4.c
@@ -5038,7 +5038,11 @@
 
     if (require_token_embd && !w->token_embd) ds4_die("required token embedding tensor is missing");
     if (w->token_embd) {
-        tensor_expect_layout(w->token_embd, DS4_TENSOR_F16, 2, DS4_N_EMBD, DS4_N_VOCAB, 0);
+        if (w->token_embd->type == DS4_TENSOR_F16) {
+            tensor_expect_layout(w->token_embd, DS4_TENSOR_F16, 2, DS4_N_EMBD, DS4_N_VOCAB, 0);
+        } else {
+            tensor_expect_dense_quant_layout(w->token_embd, 2, DS4_N_EMBD, DS4_N_VOCAB, 0);
+        }
     }
 
     const bool have_output = weights_have_output_head(w);
@@ -25829,6 +25833,7 @@
                                                  model->map,
                                                  model->size,
                                                  weights->token_embd->abs_offset,
+                                                 weights->token_embd->type,
                                                  (uint32_t)weights->token_embd->dim[1],
                                                  (uint32_t)token,
                                                      DS4_N_EMBD,
@@ -25996,6 +26001,7 @@
                                                      model->map,
                                                      model->size,
                                                      weights->token_embd->abs_offset,
+                                                     weights->token_embd->type,
                                                      (uint32_t)weights->token_embd->dim[1],
                                                      (uint32_t)token,
                                                      DS4_N_EMBD,
@@ -26045,6 +26051,7 @@
                                                      model->map,
                                                      model->size,
                                                      weights->token_embd->abs_offset,
+                                                     weights->token_embd->type,
                                                      (uint32_t)weights->token_embd->dim[1],
                                                      (uint32_t)token,
                                                      DS4_N_EMBD,
@@ -26609,6 +26616,7 @@
                                               model->map,
                                               model->size,
                                               weights->token_embd->abs_offset,
+                                              weights->token_embd->type,
                                               (uint32_t)weights->token_embd->dim[1],
                                               (uint32_t)token,
                                               DS4_N_EMBD,
@@ -26832,12 +26840,13 @@
     }
 #endif
 
-    if (tokens && n_tokens >= gpu_min) {
+    if (tokens && (weights->token_embd->type != DS4_TENSOR_F16 || n_tokens >= gpu_min)) {
         return ds4_gpu_embed_tokens_hc_tensor(out_hc,
                                                 tokens,
                                                 model->map,
                                                 model->size,
                                                 weights->token_embd->abs_offset,
+                                                weights->token_embd->type,
                                                 (uint32_t)weights->token_embd->dim[1],
                                                 n_tokens,
                                                 DS4_N_EMBD,
@@ -29672,6 +29681,7 @@
                                            model->map,
                                            model->size,
                                            weights->token_embd->abs_offset,
+                                           weights->token_embd->type,
                                            (uint32_t)weights->token_embd->dim[1],
                                            (uint32_t)token,
                                            DS4_N_EMBD,
@@ -30762,6 +30772,7 @@
                                             base_model->map,
                                             base_model->size,
                                             base_weights->token_embd->abs_offset,
+                                            base_weights->token_embd->type,
                                             (uint32_t)base_weights->token_embd->dim[1],
                                             dw->block_size,
                                             DS4_N_EMBD,
@@ -30867,6 +30878,7 @@
                                             base_model->map,
                                             base_model->size,
                                             base_weights->token_embd->abs_offset,
+                                            base_weights->token_embd->type,
                                             (uint32_t)base_weights->token_embd->dim[1],
                                             dw->block_size,
                                             DS4_N_EMBD,
@@ -32818,6 +32830,7 @@
                                                   base_model->map,
                                                   base_model->size,
                                                   base_weights->token_embd->abs_offset,
+                                                  base_weights->token_embd->type,
                                                   (uint32_t)base_weights->token_embd->dim[1],
                                                   (uint32_t)token,
                                                   DS4_N_EMBD,
@@ -34738,6 +34751,7 @@
                                                model->map,
                                                model->size,
                                                weights->token_embd->abs_offset,
+                                               weights->token_embd->type,
                                                (uint32_t)weights->token_embd->dim[1],
                                                (uint32_t)token0,
                                                DS4_N_EMBD,
@@ -34746,6 +34760,7 @@
                                                model->map,
                                                model->size,
                                                weights->token_embd->abs_offset,
+                                               weights->token_embd->type,
                                                (uint32_t)weights->token_embd->dim[1],
                                                (uint32_t)token1,
                                                DS4_N_EMBD,
@@ -58467,6 +58482,7 @@
                                                e->model.map,
                                                e->model.size,
                                                e->weights.token_embd->abs_offset,
+                                               e->weights.token_embd->type,
                                                (uint32_t)e->weights.token_embd->dim[1],
                                                (uint32_t)tokens[0],
                                                DS4_N_EMBD,
@@ -60759,6 +60775,7 @@
                 model->map,
                 model->size,
                 weights->token_embd->abs_offset,
+                weights->token_embd->type,
                 (uint32_t)weights->token_embd->dim[1],
                 (uint32_t)items[i].token,
                 DS4_N_EMBD,
@@ -61242,6 +61259,7 @@
                      e->model.map,
                      e->model.size,
                      e->weights.token_embd->abs_offset,
+                     e->weights.token_embd->type,
                      (uint32_t)e->weights.token_embd->dim[1],
                      (uint32_t)items[i].token,
                      DS4_N_EMBD,
@@ -63953,6 +63971,7 @@
                  model->map,
                  model->size,
                  weights->token_embd->abs_offset,
+                 weights->token_embd->type,
                  (uint32_t)weights->token_embd->dim[1],
                  (uint32_t)items[i].token,
                  DS4_N_EMBD,
@@ -64363,6 +64382,7 @@
                     model->map,
                     model->size,
                     weights->token_embd->abs_offset,
+                    weights->token_embd->type,
                     (uint32_t)weights->token_embd->dim[1],
                     (uint32_t)decode_items[i].token,
                     DS4_N_EMBD,
